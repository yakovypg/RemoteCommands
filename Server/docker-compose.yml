name: RemoteCommands

services:
  server:
    build: .
    container_name: RemoteCommandsServer
    restart: on-failure
    ports:
      - 8080:8080
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
